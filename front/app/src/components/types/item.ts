export type Item = {
    key: string;
    user: string;
    text: string;
    done: boolean;
    genre: string;
    // priority: number;
  };

export type User = {
  key: string;
  name: string;
  email: string;
};
