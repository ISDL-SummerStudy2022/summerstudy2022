version: '3.9'
services:
  react-app:
    build:
        context: ./frontend
        dockerfile: Dockerfile
    volumes:
      - ./frontend:/src
    command: sh -c 'cd app && PORT=80 yarn start'
    ports:
      - '80:80'
    stdin_open: true
